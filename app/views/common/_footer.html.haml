.container.main-footer
    .row.text-center
        .col-12
            = link_to t('common.all_users'), users_path, class: 'btn btn-link'
            = link_to t('common.home'), user_signed_in? ? authenticated_root_path : unauthenticated_root_path, class: 'btn btn-link'
